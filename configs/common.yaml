# ═══════════════════════════════════════════════════════════════════════════
# /opt/xbrain_v6/configs/common.yaml
#   层：L1（共享命名空间的唯一定义处）    归属进程：无归属（全部进程经 freeze 读快照）
#   ★ 权威定义处：`10` §5.4.5 共享参数唯一定义处对照表
#
#   ★★★ 本文件当前是【空骨架】—— 一个键都没有，🚫 本轮未凭空填任何配置值。
#   ★ 落值时逐键去权威节抄，🚫 不得从本文件或任何别处推断默认值：
#     10 §5.4.2 R-3 逐字「不提供默认值语法……解析不到即 E_CONFIG_INVALID 拒绝启动」。
#   ★★ 未标定的参数留 null（＝「声明键位但未赋值」），🚫 绝不写 0.0 冒充已赋值。
#   ★★ 本层只许写 `common.*`，出现其他顶层键即拒绝（10 §5.4.3 L1 行）。
# ═══════════════════════════════════════════════════════════════════════════

# TODO(`10` §5.4.5 共享参数唯一定义处对照表)：按该节逐键落值。本文件在落值前会让整栈按设计拒绝启动，这是预期行为。
