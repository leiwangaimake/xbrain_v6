# ═══════════════════════════════════════════════════════════════════════════
# /opt/xbrain_v6/configs/nav2/behavior_only.yaml
#   层：—（ROS 2 侧参数，不进 L0–L6 覆盖轴）    归属进程：Nav2 `behavior_server` ＋ 专用 lifecycle_manager
#   ★ 权威定义处：`11` §10.3（配置 `/opt/xbrain_v6/configs/nav2/behavior_only.yaml`）
#
#   ★★★ 本文件当前是【空骨架】—— 一个键都没有，🚫 本轮未凭空填任何配置值。
#   ★ 落值时逐键去权威节抄，🚫 不得从本文件或任何别处推断默认值：
#     10 §5.4.2 R-3 逐字「不提供默认值语法……解析不到即 E_CONFIG_INVALID 拒绝启动」。
#   ★★ 未标定的参数留 null（＝「声明键位但未赋值」），🚫 绝不写 0.0 冒充已赋值。
#   ⚠️★★ 参数名在 Nav2 各分支间有差异（Humble vs Jazzy），权威节自己逐字写着
#      「实现期必须以实机 Nav2 版本的 behavior_server 参数声明为准」
#      ⇒ 🚫 本轮不落值，装机确认 Nav2 版本后再抄。
# ═══════════════════════════════════════════════════════════════════════════

# TODO(`11` §10.3（配置 `/opt/xbrain_v6/configs/nav2/behavior_only.yaml`）)：按该节逐键落值。本文件在落值前会让整栈按设计拒绝启动，这是预期行为。
