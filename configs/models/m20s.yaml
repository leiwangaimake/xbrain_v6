# ═══════════════════════════════════════════════════════════════════════════
# /opt/xbrain_v6/configs/models/m20s.yaml
#   层：L2（机型层）    归属进程：同型号共用
#   ★ 权威定义处：`11` §9.6 静态规格（配置来源）
#
#   ★★★ 本文件当前是【空骨架】—— 一个键都没有，🚫 本轮未凭空填任何配置值。
#   ★ 落值时逐键去权威节抄，🚫 不得从本文件或任何别处推断默认值：
#     10 §5.4.2 R-3 逐字「不提供默认值语法……解析不到即 E_CONFIG_INVALID 拒绝启动」。
#   ★★ 未标定的参数留 null（＝「声明键位但未赋值」），🚫 绝不写 0.0 冒充已赋值。
#   ★★ 本层只许写 common.spec.* · common.motion.*（10 §5.4.3 L2 行）。
#   ⚠️★★★ 落值前先看 cannot_close：`11` §9.6 样例内 `max_vx_mps` 仍写 `0.0`，
#      而 10 §5.4.5 该行逐字要求「未落值前必须留 `null`，不得写 0.0 冒充已赋值」。
#      ⇒ 🚫 不要照抄那个 0.0。
# ═══════════════════════════════════════════════════════════════════════════

# TODO(`11` §9.6 静态规格（配置来源）)：按该节逐键落值。本文件在落值前会让整栈按设计拒绝启动，这是预期行为。
