# ═══════════════════════════════════════════════════════════════════════════
# /opt/xbrain_v6/configs/safety/clock.yaml
#   层：L3（安全层，整目录自动 locked）    归属进程：P1 速度门 · P2 健康度 · rtk_driver
#   ★ 权威定义处：`11` §1.5.5（配置项 `/opt/xbrain_v6/configs/safety/clock.yaml`）
#
#   ★★★ 本文件当前是【空骨架】—— 一个键都没有，🚫 本轮未凭空填任何配置值。
#   ★ 落值时逐键去权威节抄，🚫 不得从本文件或任何别处推断默认值：
#     10 §5.4.2 R-3 逐字「不提供默认值语法……解析不到即 E_CONFIG_INVALID 拒绝启动」。
#   ★★ 未标定的参数留 null（＝「声明键位但未赋值」），🚫 绝不写 0.0 冒充已赋值。
#   ★★ 本层只许写 common.safety.*（10 §5.4.3 L3 行 · 10 §5.4.5 逐字 `common.safety.clock.*`）。
#   命名空间分歧已裁（99 U73, 2026-08-06 用户拍板）：键路径 = common.safety.clock.*
#      （10 §5.4.5 已登记, 11 §1.5.5 yaml 样例已对齐嵌套到 common.safety.clock）。
#      本文件仍留空 —— 原因改为【值待标定】：sync_timeout_s / offset_threshold_ms 等按
#      11 §1.5.5 逐键抄, rtc_trusted 待确认板载 RTC 电池, 未标定留 null（§3.1）。
#   ★★ 本文件不跟随 XBRAIN_CONFIG_DIR（10 §5.4.0 ENV-2）。
# ═══════════════════════════════════════════════════════════════════════════

# TODO(`11` §1.5.5（配置项 `/opt/xbrain_v6/configs/safety/clock.yaml`）)：按该节逐键落值。本文件在落值前会让整栈按设计拒绝启动，这是预期行为。
