# RNS 开发编排（RNS_TODO）

| 项 | 内容 |
|---|---|
| 依据 | `20-RNS反应式导航软件系统详细设计.md` **v1.11（已三遍审核 ＋ 量化收口 · 可开工）** · `11` §3.1B v1.9 · `CLAUDE.md` |
| ★★★ 仓库前提 | **存量改造，🚫 不是绿地**：`xbrain/p1_motion/` 已有 66 文件 —— 含 **v0.2 时代的 `rns/`（11 文件 ＋ 30 条绿测试，建立在已作废的 `rt/lidar/grid` 设计上）**、待删改的 `path/path_follow.py`/`target_oriented.py`/`nav2_proxy.py`、订**旧 key** 的 `perception_src/`。⇒ 一切从 **Phase −1 存量处置**起步 |
| 建档 | 2026-09-08 |
| 性质 | ★ **执行编排**，🚫 不是设计文档 —— 任何设计疑问回 `20`，两处冲突以 `20` 为准 |
| 完备性判据 | ★★★ 附录 A 覆盖对账表：`20` 的**每一个** `RNS-M/N/I/T` 号与断言族都映射到唯一任务；由元任务 **M-2** 的脚本现场求值，🚫 手数 |

## 0. 全程铁律（每个 Phase 都适用）

| # | 规则 | 出处 |
|---|---|---|
| G-1 | 🚫 **无工作流**：手工顺序 batch，一批一个提交，跑完 lint ＋ 测试 ＋ 变异体再进下一批 | `CLAUDE.md` 铁律 -1 |
| G-2 | 提交判据用 `&&` 串成**门**：`charset_lint && pytest && git commit` | `CLAUDE.md` §8.1 |
| G-3 | ★★★ 变异体运行前后**清 `__pycache__`**（sshfs mtime 1 s，等长变异会给假结果） | `CLAUDE.md` §7.2.1 |
| G-4 | 每条断言**先红后绿**：先写变异体看它红，再写实现看它绿 | `CLAUDE.md` §3.3 |
| G-5 | 注释率 ≥ 70%，注释反向链接节号（`# 见 20 §7.3 ②'`） | `CLAUDE.md` §2.4 / §9.2 |
| G-6 | 配置零默认兜底：`null` ⇒ 拒启并报键路径；🚫 `0.0` 冒充 | `CLAUDE.md` §3.1 |
| G-7 | 改签名逐调用点遍历并交代无覆盖点 | `CLAUDE.md` §9.1A |
| G-8 | 循环内零阻塞 I/O、零 Zenoh、零线程（RNS-M-1/2/4/5 的日常形态） | `20` §1.1 |
| ★★ G-9 | **编排与审查必须对仓库实况**，🚫 只对设计文档 —— 本文件初版按绿地编排、两轮三遍审查都没抓到，因为审的全是 TODO↔20 一致性，没人对过 TODO↔repo（2026-09-08 教训） | 本文件 v2 |

## 1. 阻塞依赖总表（开工前先看这张）

| 依赖 | 阻塞谁 | 状态 |
|---|---|---|
| **#20-1** `12` 修订：删 `path_follow` · 行为源清单/优先级重排 · `rns_avoid(900)` 接口确认 | P0.4 接线 · P7.2 | ⚠️ 交办 `12`，未落 |
| **#20-3** `12` §12 落 `rns.yaml` 全部键的定义处 | P0.5 | ⚠️ 交办 `12`，未落 |
| **#20-14** `p3_task` 路网规划器（L3） | 仅 P7.3 | ⚠️ 交办 `15`，未落 |
| perception 真数据（`19` §15 W-1~W-10） | 仅 P7.1（P0~P6 全用合成/模拟） | ⚠️ 感知方在办 |
| Phase −1 处置表（PM1.1~PM1.5） | P0.2 / P0.4（改造蓝本与落点） | ★ 本编排内, 开工即做 |
| **#20-4/#20-5** 相机安装几何 · `accel/wz_max` 标定 | 仅 P7 实机整定 | ⚠️ 整机侧 |
| `12` §6A 旋转许可（已有设计） | P4.5 集成点 | ✅ 设计在册 |

★★★ **P0~P6 不被外部阻塞** —— 感知输入全程用合成 `ProfileMsg`/`ObjectsMsg`（P0.6 建，与 `19` W-11 样例集共源）。

---

## Phase −1 · 存量审计与处置（v2 新增 —— 一切之前）

> 出口 Gate：差量处置表落档（逐文件三选一：**复用 / 改造 / 作废**，作废必带测试墓碑策略）；`12` 侧 #20-1 的**代码半边**排进同一批。

| # | 任务 | 对象（实测清单） | 验收 |
|---|---|---|---|
| **PM1.1** | 存量盘点：`xbrain/p1_motion/` 66 文件 ↔ `12`（现行）/`20` v1.11 逐目录差量表，产出 `docs/rns-legacy-audit.md` | 全目录（`arb/ gate/ path/ perception_src/ profile/ rns/ rotation/ route/ sources/ teleop/ runtime/ fence/ freshness/ config/ …`） | 差量表评审 |
| **PM1.2** | ★★★ 旧 `rns/` 逐文件判决：`grid_motion.py`（`rt/lidar/grid` 补偿 —— 前提已死）· `corridor.py` · `targets_veto.py` · `inflate.py` · `candidate_gen.py` · `side_select.py` · `u54_semantic.py` · `audit_ring.py` · `module.py` · `shutdown.py` —— 每个标 复用/改造/作废；★ `tests/p1_motion/test_batch_d_rns.py` 30 条逐条：迁移 / 作废（作废留墓碑注释，🚫 静默删） | `xbrain/p1_motion/rns/` | 判决表 ＋ 测试迁移清单 |
| **PM1.3** | ★★ `path_follow.py` / `target_oriented.py` / `nav2_proxy.py` / `relative_move.py` 的**代码侧**处置（删源 / 归并 / 优先级重排落 `sources/arbiter_p1.py`）—— 与 #20-1 **文档批同一个提交序列**，🚫 文档删了代码还在 | `xbrain/p1_motion/path/` · `sources/` | 与 #20-1 同批合入 |
| **PM1.4** | `perception_src/` 改造设计：旧 `rt/perception/targets` 订阅 → 三新 key（保留 Replay 注入点形态 —— 那是测试的命根）；此项是 P0.2 的**改造蓝本** | `xbrain/p1_motion/perception_src/` | 设计段落入差量表 |
| **PM1.5** | 命名冲突决：`20` §1.2 新 13 文件 vs 旧 `rns/` 11 文件。★ **审计后修订（2026-09-08）**：旧件全部未接线（宿主零 import，实测），但**复用件的 9 条迁移测试**若先迁 `_legacy/` 会造出对过渡目录的依赖 ⇒ **迁移执行并入 P0.4 首提交**（复用件直接就位新名、作废件进 `_legacy/` 带墓碑、测试同批改址） | 落点冲突面 · [rns-legacy-audit](rns-legacy-audit.md) §6 | 随 P0.4 |

## Phase 0 · 前置与脚手架

> 出口 Gate：p1 携 RNS 骨架可启动；三条感知 key 进本拍快照；一条样例断言完成"先红后绿"演示；元脚本 M-1/M-2 挂进 CI。

| # | 任务 | 依据 | 验收 |
|---|---|---|---|
| **P0.1** | 催办 #20-1 / #20-3（`12` 侧两批修订）—— RNS 侧同步评审其产物 | `20` §15 | `12` 变更记录落档 |
| **P0.2** | ★ **改造** `perception_src/`（按 PM1.4 蓝本）：旧 targets 订阅换三新 key，保留 Replay 注入点；`SubscriberRegistry` 强引用 · `publish_threadsafe` · 进 `12` §2.2 步骤 1 本拍快照 | `11` §2.2.1 · `CLAUDE.md` §4.2/§4.3 | 快照含三键；T-50~T-53 计龄字段就位；Replay 路径不破 |
| **P0.3** | 快照白名单/启动自检对齐（p1 起来即校验声明 key 集） | `11` §1.1.3 v1.7 行 | 自检绿 |
| **P0.4** | 按 PM1.2/PM1.5 处置表**改造 ＋ 新建**至 `20` §1.2 全部文件形态（复用件就地归位、作废件已迁 `_legacy/`）；`source.py` 四接口空实现，`is_active()` 恒 `false`（🚫 未接线不冒烟）；`types.py` 落闭集与 mission 枚举（follow_target 仅预留值） | `20` §1.1/§1.2 ＋ PM1 处置表 | 结构在位；p1 行为不变；unwired 基线同步 |
| **P0.5** | 配置消费：读 resolved 快照 · `null` 拒启报键路径 · **D2 启动断言**（`leave_progress_m < 2·r_eff`）· `class_map` person 锁死；`config.py` | `20` §12 · §7A.1 D2 · §5.1.1 | **A-CVG-5 · A-CLS-4** 先红后绿 |
| **P0.6** | 测试脚手架：合成 `ProfileMsg`/`ObjectsMsg` 构造器（场景库骨架，见 §M-3）· mutation runner（含 G-3 清缓存）· CI 注册 | `20` §13 | 样例断言演示红→绿 |
| **P0.7** | 元脚本落地：**M-1 / M-2**（见 §元任务） | `20` §13 · 本文附录 A | 两脚本自测（self-test 植入必红） |

## Phase 1 · L1/L2 折线跟随核心（无避障）

> 出口 Gate：仿真直线 / 多段折线 / 环形路径三场景全绿；A-RT · A-ALN · A-DEV 族全部先红后绿。

| # | 任务 | 依据 | 验收 |
|---|---|---|---|
| **P1.1** | 弧长投影 `s` ＋ 索引窗口单调不回退（`RNS-N-4`）；`route.py` | `20` §2.2/§2.6 | **A-RT-3 · A-RT-4** |
| **P1.2** | 前视点 R 与 `L = clamp(k·v, L_min, L_max)`（pure pursuit [10]） | `20` §2.2 | 曲率跟踪金标 |
| **P1.3** | goto/path 统一为折线跟随（`RNS-N-1`）；中间点 🚫 不判到达（`20` §2.4） | `20` §2.1/§2.3/§2.4 | **A-RT-1 · A-RT-2** |
| **P1.4** | 渐进对准（`RNS-N-3`）：`align_dist_m` 内 Δθ 收敛、`ω_req > wz_max` 压速 | `20` §2.5 | **A-ALN-1 · A-ALN-2** |
| **P1.5** | 偏离上限 `max_deviation_m` ⇒ 失败上报（含归因） | `20` §2.7 · §9.2 | **A-DEV-1** |
| **P1.6** | 循环遍历（§2.8）与 `path_points`/`waypoint_ids` 分工（§2.9）；重入语义（`RNS-N-2`） | `20` §2.3/§2.8/§2.9 | 环形场景 |
| **P1.8** | 接管 `12` §3.5A 路径指针消费 ＋ `route_rev` 交叉校验（`11` §7.12.1）—— `path_follow` 删除后 RNS 是唯一消费者；`route.py`。★ 只读实现可先行，行为接线随 P7.2 | `12` §3.5A · `11` §7.12.1 | rev 失配拒执单测 |
| **P1.7** | `speed.py` 骨架：`v = min(...)` 结构 ＋ 本 phase 可用项（`f(d_free)`前向 min · 围栏 · 航向 `i_heading` · `routes.max_speed` · 横向偏差 · 对准压速）＋ §8.2 航向能力收缩 | `20` §8.1/§8.2 | **A-SPD-1 · A-SPD-2**（结构性）；全 11 项在 P6 收口 |

## Phase 2 · 三态融合 · 记忆栅格 · 感知健康

> 出口 Gate：玻璃洞 / 盲区转向 / 无分割三金标全绿；A-FUS · A-MEM · A-RTK 族收口。

| # | 任务 | 依据 | 验收 |
|---|---|---|---|
| **P2.1** | `profile` 消费纪律：`null` 保持 UNKNOWN（`RNS-I-1`）· `d_free≤d_block` 消费校验（`RNS-I-3`）· 双报文各自计龄（`RNS-I-4`，T-50/51/52/53 行为）；`inputs.py` | `20` §3.1 · `11` §1.6 | **A-FUS-3 · A-FUS-4 · A-FUS-5** |
| **P2.2** | 不对称融合（`RNS-N-5`）＋ 每拍重跑（§4.1）；`grid.py` 三态 | `20` §3.1.2 · §4.1 | **A-FUS-1 · A-FUS-2** |
| **P2.3** | T 通道降级：bit0 逐 bin 限速 · `seg_stale_ms`/`no_seg_speed_cap` · 恢复迟滞 | `20` §3.1.11 | **A-FUS-6** |
| **P2.4** | `invalid_ratio_limit` 强制限速（`RNS-I-2`）＋ `status` 消费（T-53 最坏限速） | `20` §3.1.8 | FUS 族补测 |
| **P2.5** | 记忆栅格：RTK 锚定 · `ttl_static/dynamic` · 时变近场盲区标记（`RNS-I-5`）· 未观测近场禁转向/横移（`RNS-I-6`） | `20` §4.2/§4.3 | **A-MEM-1 · A-MEM-2** |
| **P2.6** | 定位输入与 `RNS-N-6` 三档（SINGLE ⇒ 停车上报）；UNKNOWN 占比限速项入 §8.1 | `20` §3.2/§3.2.1 · §8.1 | **A-RTK-1 · A-RTK-2** |
| **P2.7** | 围栏输入消费（§3.3）＋ 本拍快照纪律核查（`RNS-M-4` 不自读） | `20` §3.3/§3.4 | 元测试 |

## Phase 3 · 障碍分类 · 动态规则 · 停等预算

> 出口 Gate：堵门 / 轮换堵门 / 起步车三金标全绿；A-CLS · A-DYN · A-CVG-3 收口。

| # | 任务 | 依据 | 验收 |
|---|---|---|---|
| **P3.1** | `objects` 消费与 `class_map` 分流（`RNS-N-7`）：未映射 ⇒ `block`＋限速；`velocity_frame == raw` 拒用（§3.1.5） | `20` §5.1/§5.1.1 | **A-FUS-7 · A-CLS-4**（P0.5 已红绿，此处接真逻辑） |
| **P3.2** | `person` 恒停不绕不穿（§5.2）；静止判据 = 阈值＋驻留（§5.5，`RNS-N-8` 占道定义） | `20` §5.2/§5.4/§5.5 | **A-CLS-1 · A-CLS-2 · A-CLS-3** |
| **P3.3** | 动态距离规则双阈值（§5.3）＋ **停等预算**（`RNS-N-16`：计时绑状态 · 重分类联动 · `blocked_by_dynamic` 上报）；`dynamic.py` | `20` §5.3/§5.3A | **A-DYN-1 · A-DYN-2 · A-CVG-3（×3 变异）** |
| **P3.4** | 分类型安全余量 `margin_by_class` 膨胀（§5.6，U54 单向约束折入）；`classify.py` | `20` §5.6 | 余量金标 ＋ 属性测试 |

## Phase 4 · 穿行/绕行候选

> 出口 Gate：双缝 / 斜窄缝 / 格中部障碍 / 假缝（未识别杆）四金标全绿；A-GAP · A-HYS 族收口。

| # | 任务 | 依据 | 验收 |
|---|---|---|---|
| **P4.1** | 剖面边缘不连续 → 子目标外推（`RNS-N-9`，`edge_jump_m`）；`candidate.py` | `20` §6.1 | **A-GAP-1 · A-GAP-2** |
| **P4.2** | 硬门集（§6.2）＋ 宽度硬门＋饱和（`RNS-N-10`，`gate_saturate_ratio`） | `20` §6.2/§6.3 | **A-GAP-3 · A-GAP-4** |
| **P4.3** | 代价函数五权重（§6.4）＋ 换候选迟滞 `side_hold_ticks`（§6.6） | `20` §6.4/§6.6 | **A-GAP-5 · A-GAP-6 · A-HYS-1 · A-HYS-2** |
| **P4.4** | 穿插只在静止障碍间（`RNS-N-11`）——动态一律 §5.3 距离规则 | `20` §6.1 | CLS/GAP 交叉金标 |
| **P4.5** | `wz` 出口接 `12` §6A 旋转许可（`r_eff` 唯一实现处 §1.3）；🚫 本模块不复制判定 | `20` §1.3 · `12` §6A | **A-GEO-1**（r_eff 定义点恰 1 处，元测试）＋ 集成测试（fake 底盘） |

## Phase 5 · 贴墙 · 收敛闭环 · 急停

> 出口 Gate：U 形 / 螺旋 / 环形路径贴墙 / 擦边 / 纯 UNKNOWN 停滞五金标全绿；A-WF · A-BK · A-CVG-1/2/4/6 · A-ES 全族先红后绿 —— **`RNS-T-1` 的全部机件到位**。

| # | 任务 | 依据 | 验收 |
|---|---|---|---|
| **P5.1** | 贴墙进入条件与选边（§7.2：无可行候选才进）；`wallfollow.py` 状态机 | `20` §7.1/§7.2 | **A-WF-1** |
| **P5.2** | 贴墙控制律（`d_wall_m`，§7.7 两易错点；D1：贴墙距离落速度门非零段）＋ 等待期保持（§7.8） | `20` §7.7/§7.8 · §7A.1 D1 | **A-WF-2 · A-WF-3** |
| **P5.3** | 退出判据 ①②'③（§7.3 弧长版）＋ `s_hit` 定位系记账 ＋ **D3 穿出点记录与整圈放宽** | `20` §7.3 · §7A.1 D3 | **A-CVG-1 · A-CVG-4 · A-CVG-6 · A-WF-4** |
| **P5.4** | 失败三判据（§7.6：回 H 整圈【先查 D3】/ 上确界无改善 / 里程兜底）＋ 归因上报 | `20` §7.6 · §9.2 | **A-WF-5 · A-WF-6** |
| **P5.5** | 贴墙依赖记忆（§7.4）＋ 限定条件放宽后退（`RNS-N-13`，`backup` 键组，仅最近观测 FREE 区） | `20` §7.4/§7.5 | **A-BK-1 · A-BK-2** |
| **P5.6** | **看门狗**（`RNS-N-15`）：进度量/复位/触发 · 双分支（有墙升级 / 无墙报 `watchdog_no_progress`）· argmax 限速源筛（引 `12` §6.8 闭集）；`watchdog.py` | `20` §7.3A | **A-CVG-2（×2 变异）** |
| **P5.7** | 行为源生命周期收口：**急停 `RNS-M-7`**（挂起零输出 · 解除前不活跃 · 解除后重求值，堵"松开即冲车"）＋ `on_preempted`/`on_input_lost` 的状态处置语义（贴墙/停等状态的挂起与作废）；`source.py` | `20` §1.1 RNS-M-3/M-7 | **A-ES-1 · A-ES-2** ＋ 抢占场景单测 |

## Phase 6 · 失效上报 · 审计 · 实时化收口

> 出口 Gate：20 Hz 全链路仿真回放 P99 达标，**A-PERF-1/2 摘除 `xfail` 转绿**（#20-8 期限兑现）；失败归因闭集全路径演示。

| # | 任务 | 依据 | 验收 |
|---|---|---|---|
| **P6.1** | 失败上报通道收口：**§9.0.2 八值归因闭集**（`types.py` 导出）＋ 状态机闭集（§9.0.1）＋ 终态/取消/替换语义（§9.0.3）—— 🚫 布尔上报 🚫 自动重试 | `20` §9.0~§9.2 | **A-ST-1 · A-FAIL-1/2/3** ＋ 逐因注入 |
| **P6.2** | 审计环形缓冲＋非实时线程搬运（`RNS-M-2`：循环内零落盘） | `20` §9.3 | 注入 `raise` ⇒ 本拍零速＋下拍仍跑（`CLAUDE.md` §4.4） |
| **P6.3** | 逐格运算向量化（§10.2）＋ 预算达标；`v = min(...)` 全 11 项复核收口（§8.1 逐项打勾） | `20` §10.2 · §8.1 | **A-PERF-1 · A-PERF-2** 转绿 |
| **P6.4** | 单调钟静扫面确认覆盖 `rns/`（CI 既有）＋ 模块内零墙钟 | `20` §10.1 | **A-CLK-1** |
| **P6.5** | doccheck 挂接确认（`20` 在 `scan_manifest.json`） | `20` §13 | **A-SCAN-1** |

## Phase 7 · 集成联调（外部依赖就绪后逐项解锁）

| # | 任务 | 解锁条件 | 验收 |
|---|---|---|---|
| **P7.1** | 真 perception 对接：三 key 实流 · T-50~53 实测年龄 · sim→real 切换开关下线 | `19` W-1~W-10 交付 | 实机金标复跑 |
| **P7.2** | `12` 仲裁集成：`rns_avoid(900)` 激活/抢占/部分抑制（RRA）· teleop/estop 交叉场景 | **#20-1 落档** | 集成场景表 |
| **P7.3** | L3 端到端：p3 路网折线 → RNS 执行 · 脱网回退(a) 演练 | **#20-14 交付** | **A-NET-1**（p3 侧）＋ 全链金标 |
| **P7.4** | 实机整定：`rns.yaml` 全部 `null` 落值（含 D2 校验）· 记录整定依据 | #20-4/#20-5 标定 | 配置评审 |
| **P7.5** | 变异体全量重跑（G-3 纪律）＋ §14 陷阱表逐条 review 打勾 | P0~P6 全绿 | 全量报告 |

## 元任务（完备性的机器判据，🚫 靠人数）

| # | 任务 | 判据 |
|---|---|---|
| **M-1** | `scripts/doccheck/rns_assert_cover.py`：`20` §13 每个断言 ID ↔ `tests/p1_motion/rns/` 内同名测试标记，双向差集 = 空；`--self-test` 植入必红（仿 MUT-COVER 形制，独立脚本 🚫 扩其扫描面） | CI 门禁 |
| **M-2** | `scripts/doccheck/rns_todo_cover.py`：本文附录 A ↔ `20` 全文 `RNS-[MNIT]-n` 与断言族全集，双向差集 = 空 —— **本文件"无遗漏"的机器证明** | CI 门禁 |
| **M-3** | 金标场景库（P0.6 起建，随 phase 增量）：直线 · 多段折线 · 环形路径 · 玻璃洞 · 盲区转向 · 无分割 · 堵门 · 轮换堵门 · 起步车 · 双缝 · 斜窄缝 · 格中部障碍 · 假缝(未识别杆) · U 形 · 螺旋 · 擦边 · 环形贴墙 · 纯 UNKNOWN 停滞 · 曲率跟踪与渐进对准 · seg 断供后恢复迟滞 · 直墙保距 · 凹角（内角）· 凸角过冲 · **围栏截断前向**（v1.13 反哺景：无候选无墙 ⇒ 分支二上报） —— 每场景文件头注明它服务的断言 ID | 场景↔断言映射进 M-1 扫描 |

## 预留区（🚫 本编排不排期，触发条件明示）

| 项 | 触发 |
|---|---|
| `follow_target`（§2.10 全节，断言 **A-FT-1~6**） | 用户立项 ＋ 复核 #20-12 re-ID（`20` #20-13） |
| SLAM 定位支 | `20` #20-6 |
| L3 边权扩展（围栏/历史惩罚） | `20` §2.11.4 二期 |

---

## 附录 A · 覆盖对账表（M-2 脚本的扫描对象）

**裁决/不变量 → 任务**：

| ID | 任务 | ID | 任务 | ID | 任务 | ID | 任务 |
|---|---|---|---|---|---|---|---|
| RNS-M-1 | P0.4 | RNS-M-2 | P6.2 | RNS-M-3 | P0.4 | RNS-M-4 | P2.7 |
| RNS-M-5 | P0.4 | RNS-M-6 | P7.2 | RNS-M-7 | P5.7 | RNS-N-1 | P1.3 |
| RNS-N-2 | P1.6 | RNS-N-3 | P1.4 | RNS-N-4 | P1.1 | RNS-N-5 | P2.2 |
| RNS-N-6 | P2.6 | RNS-N-7 | P3.1 | RNS-N-8 | P3.2 | RNS-N-9 | P4.1 |
| RNS-N-10 | P4.2 | RNS-N-11 | P4.4 | RNS-N-12 | P5.3 | RNS-N-13 | P5.5 |
| RNS-N-14 | 预留区 | RNS-N-15 | P5.6 | RNS-N-16 | P3.3 | RNS-N-17 | P7.3 |
| RNS-I-1 | P2.1 | RNS-I-2 | P2.4 | RNS-I-3 | P2.1 | RNS-I-4 | P2.1 |
| RNS-I-5 | P2.5 | RNS-I-6 | P2.5 | RNS-I-7 | P2.5（建）→ P5.5（贴墙前提核验） | RNS-T-1 | P5（Gate） |

**断言族 → 任务**（族内逐条由 M-1 对账）：

| 族 | 任务 | 族 | 任务 | 族 | 任务 |
|---|---|---|---|---|---|
| A-RT | P1.1/P1.3 | A-ALN | P1.4 | A-DEV | P1.5 |
| A-SPD | P1.7/P6.3 | A-FUS | P2.1~P2.4/P3.1 | A-MEM | P2.5 |
| A-RTK | P2.6 | A-CLS | P0.5/P3.1/P3.2 | A-DYN | P3.3 |
| A-GEO | P4.5 | A-GAP | P4.1~P4.3 | A-HYS | P4.3 |
| A-WF | P5.1~P5.4 | A-BK | P5.5 | A-CVG | P0.5/P3.3/P5.3/P5.6 |
| A-ES | P5.7 | A-PERF | P6.3 | A-CLK | P6.4 |
| A-ST | P6.1 | A-FAIL | P6.1 | — | — |
| A-SCAN | P6.5 | A-NET | P7.3 | A-FT | 预留区 |

**`rns.yaml` 键组 → 任务**：`route:`→P1 · `dynamic:`→P3.3 · `watchdog:`→P5.6 · `clearance:`→P3.4/P4.2 · `cost_weights:`/`candidate:`→P4 · `wall_follow:`/`backup:`→P5 · `memory:`/`health:`/`perception:`→P2 · `class_map:`→P3.1 · `follow:`→预留区。

上海哈船智能船舶技术有限公司 · XBRAIN_V6
